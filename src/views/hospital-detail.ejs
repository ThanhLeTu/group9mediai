<div class="text-center mt-12 px-4">
    <h2 class="text-4xl font-bold text-blue-800 mb-2">🏥 <%= hospital.name %></h2>
    <p class="text-gray-600"><i>📍 <%= hospital.address %></i></p>
  </div>
  
  <!-- Danh sách bác sĩ -->
  <div class="text-center mt-10 px-4">
    <h3 class="text-2xl font-semibold text-indigo-700 mb-6">👨‍⚕️ Bác sĩ tại bệnh viện</h3>
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
      <% doctors.forEach(doctor => { %>
        <div class="bg-white rounded-xl shadow hover:shadow-md overflow-hidden border transition-all">
          <img src="<%= doctor.avatar || '/static/images/default-doctor.jpg' %>" alt="Ảnh bác sĩ"
               class="w-full h-48 object-cover">
  
          <div class="p-4 text-left">
            <h4 class="text-lg font-semibold text-gray-800 mb-1"><%= doctor.fullName %></h4>
            <p class="text-sm text-gray-500 italic mb-2">🩺 <%= doctor.specializationId?.name || 'Chuyên khoa khác' %></p>
            <a href="/booking/<%= doctor._id %>" class="inline-block px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700 text-sm">
              Đặt khám
            </a>
          </div>
        </div>
      <% }) %>
    </div>
  </div>
  
  <!-- Danh sách chuyên khoa -->
  <div class="text-center mt-16 px-4 pb-16">
    <h3 class="text-2xl font-semibold text-purple-700 mb-4">📚 Các chuyên khoa có tại bệnh viện</h3>
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4">
      <% specializations.forEach(spec => { %>
        <div class="bg-gray-100 rounded-xl p-4 text-left shadow-sm">
          <h4 class="text-base font-semibold text-purple-800 mb-1"><%= spec.name %></h4>
          <p class="text-sm text-gray-600"><%= spec.description || 'Không có mô tả' %></p>
        </div>
      <% }) %>
    </div>
  </div>
  