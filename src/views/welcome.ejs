<div class="text-center mt-20">
  <h1 class="text-3xl font-bold text-blue-700">Chào mừng <%= email || 'bạn' %> đến với Medi9 Health</h1>
  <p class="mt-4 text-gray-600">Hãy đăng nhập để sử dụng các tính năng khám bệnh và quản lý hồ sơ.</p>
</div>
<div class="text-center mt-10">
  <h2 class="text-2xl font-bold text-blue-800 mb-6">👨‍⚕️ Danh sách bác sĩ nổi bật</h2>

  <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6 px-4">
    <% doctors.forEach(doctor => { %>
      <div class="bg-white rounded-xl shadow-md overflow-hidden transition hover:shadow-lg border">
        <img src="<%= doctor.avatar || '/static/images/default-doctor.jpg' %>" alt="Ảnh bác sĩ" class="w-full h-48 object-cover">

        <div class="p-4 text-left">
          <div class="flex items-center text-green-600 text-sm mb-1">
            <span class="w-2 h-2 rounded-full bg-green-500 mr-2"></span>
            Available
          </div>

          <h3 class="text-lg font-semibold text-gray-800 truncate">
            <%= doctor.fullName || doctor.email %>
          </h3>

          <p class="text-sm text-gray-500">
            <%= doctor.specializationId?.name || 'Chuyên khoa khác' %>
          </p>
        </div>
      </div>
    <% }) %>
  </div>
</div>

<div class="text-center mt-12">
  <h2 class="text-2xl font-bold text-indigo-700 mb-6">🏥 Danh sách bệnh viện</h2>

  <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6 px-4">
    <% hospitals.forEach(hospital => { %>
      <div class="bg-white rounded-lg shadow p-4 text-left border hover:shadow-lg transition">
        <h3 class="text-lg font-bold text-blue-800"><%= hospital.name %></h3>
        <p class="text-sm text-gray-600 mt-1">
          📍 <%= hospital.address || 'Địa chỉ chưa cập nhật' %>
        </p>
        <p class="text-sm text-gray-500">
          📞 <%= hospital.phone || 'Chưa có số điện thoại' %>
        </p>
      </div>
    <% }) %>
  </div>
</div>

<div class="mt-10 px-4">
  <h2 class="text-2xl font-semibold text-gray-800 mb-4">🩺 Danh sách chuyên khoa</h2>

  <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
    <% specializations.forEach(spec => { %>
      <div class="bg-white rounded-lg shadow p-4 border hover:shadow-md transition">
        <h3 class="text-lg font-bold text-indigo-600"><%= spec.name %></h3>
        <p class="text-sm text-gray-600 mt-2"><%= spec.description %></p>
      </div>
    <% }); %>
  </div>
</div>
<include src="partials/footer.ejs" />