<!-- Welcome Card -->
<div class="bg-teal-500 p-6 rounded-lg mb-6">
  <div class="flex items-center">
    <div class="flex-1">
      <h2 class="text-2xl font-bold mb-2">Xin chào Admin!</h2>
      <p>Dưới đây là các thống kê quan trọng và hoạt động hôm nay.</p>
    </div>
    <img src="https://storage.googleapis.com/a1aa/image/mI4uO94b5eQ0yWPBNJCJr6axxaRPdP94FftVyS58fdE.jpg" width="100" height="100" alt="Doctors illustration" class="w-24 h-24" />
  </div>
</div>

<!-- Stats Cards -->
<% const icons = ['fa-user-injured', 'fa-user-md', 'fa-hospital', 'fa-notes-medical', 'fa-calendar-check', 'fa-syringe']; %>
<% const titles = ['👥 Tổng bệnh nhân', '🧑‍⚕️ Bác sĩ', '🏥 Bệnh viện', '📋 Hồ sơ bệnh án', '📅 Hẹn khám hôm nay', '💉 Lịch tiêm hôm nay']; %>
<% const values = [stats.totalPatients, stats.totalDoctors, stats.totalHospitals, stats.totalRecords, stats.appointmentsToday, stats.vaccinationsToday]; %>
<% const colors = ['text-teal-400', 'text-blue-400', 'text-yellow-400', 'text-green-400', 'text-indigo-400', 'text-pink-400']; %>

<% // ✅ Push thêm các thống kê mở rộng trước khi vòng lặp %>
<% titles.push('✅ Đã khám'); values.push(stats.completedAppointments); icons.push('fa-check'); colors.push('text-lime-400'); %>
<% titles.push('⏳ Tiêm sắp tới'); values.push(stats.upcomingVaccinations); icons.push('fa-clock'); colors.push('text-orange-400'); %>
<% titles.push('🛡️ Quản trị viên'); values.push(stats.totalAdmins); icons.push('fa-user-shield'); colors.push('text-cyan-400'); %>

<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-6">
  <% for (let i = 0; i < titles.length; i++) { %>
    <div class="bg-gray-800 p-6 rounded-lg shadow-md">
      <div class="flex items-center">
        <div class="flex-1">
          <h3 class="text-lg font-semibold"><%= titles[i] %></h3>
          <p class="text-2xl mt-2"><%= values[i] %></p>
        </div>
        <i class="fas <%= icons[i] %> text-3xl <%= colors[i] %>"></i>
      </div>
    </div>
  <% } %>
</div>

<!-- Doctor List (Styled like UI image) -->
<div class="bg-gray-800 p-6 rounded-lg mb-6">
  <div class="flex justify-between items-center mb-4">
    <h3 class="text-white text-lg font-semibold">Doctor List</h3>
    <button class="text-white text-xl">›</button>
  </div>
  <ul class="space-y-4">
    <% doctors.slice(0, 5).forEach(doc => { %>
      <li class="flex items-center justify-between">
        <div class="flex items-center space-x-4">
          <img src="<%= doc.avatar || 'https://via.placeholder.com/40' %>" class="w-10 h-10 rounded-full border-2 border-white">
          <div>
            <p class="text-white font-semibold">Dr. <%= doc.fullName %></p>
            <p class="text-sm text-blue-400"><%= doc.specializationId?.name || 'N/A' %></p>
          </div>
        </div>
        <span class="text-white text-lg">⋯</span>
      </li>
    <% }) %>
  </ul>
</div>